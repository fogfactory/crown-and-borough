# Prompt P1.2b — Lieux-dits → Villages (infrastructure + villages neutres)

```
Tu travailles sur "Crown & Borough". Le refactor de vocabulaire P1.2a est
fait (troupe/armée), la génération de carte existe (P1.2, internal/engine/
mapgen, endpoint /api/map), les modèles aussi (internal/models), le front
(MapViewer) affiche la carte et sa fixture d'état.
Références : specs/gdd.md (§3 Carte/Terrains, §7 Infrastructures, §8
Carence), specs/architecture.md (contrat map.json), specs/roadmap.md (P1.2
détaillé), specs/prompts/p1.1-modeles-metier.md et p1.6-hiver-investissements.md.

Tâche : supprimer la notion de "lieu-dit" et introduire le VILLAGE comme
infrastructure. Décisions actées :
- Le flag lieuDit disparaît partout (modèles, map.json, front, tests).
- Nouvelle infrastructure "village" (InfraType village) : produit 2 rations
  par tour (règle vivrière détaillée dans un prompt ultérieur, P1.2e) et
  sert d'ANCRE de ravitaillement, tout comme un château.
- Les villages sont NON constructibles par les joueurs au MVP (réservé) :
  seuls des villages NEUTRES apparaissent sur la carte initiale.
- Une ligne de ravitaillement part désormais du château OU village contrôlé
  le plus proche (le GDD §5 est reformulé en conséquence ici ; les règles
  détaillées du ravitaillement sont réécrites par P1.2e).
- LES INFRASTRUCTURES N'ONT PAS DE PROPRIÉTAIRE : seul le TERRITOIRE porte
  un propriétaire (TerritoryState.OwnerID). Infrastructure.OwnerID est
  SUPPRIMÉ du modèle. Une infrastructure appartient à sa case ; celui qui
  contrôle la case en bénéficie. Conséquences :
  * un village NEUTRE = infrastructure "village" sur un territoire NON
    contrôlé (OwnerID nil du territoire) — représentable sans modèle
    exotique ;
  * "infrastructure du joueur" (dépôt, tour de guet, moulin) se lit
    désormais "infrastructure sur une case contrôlée par le joueur" ;
  * construire un château sur une case portant un village REMPLACE le
    village (règle de la structure unique intacte : jamais 2
    infrastructures par case — donc jamais de cumul des 2 rations) ;
  * un village peut être détruit par pillage comme toute infrastructure.

RÈGLE DE CODE : code EXCLUSIVEMENT en anglais ; contenu de jeu en français.

1. MODÈLES (internal/models) :
   - Supprime Territory.IsLieuDit (et le JSON "lieuDit") de models.Territory.
   - Supprime Infrastructure.OwnerID (champ et JSON "owner") ; mets à jour
     le commentaire "Owners keep their construction even on neutral or
     hostile territory" (la propriété des constructions disparaît : une
     infrastructure appartient à sa case, le contrôle de la case fait foi)
     et la validation correspondante (plus de vérification d'un owner
     d'infrastructure dans Validate).
   - Ajoute "village" aux valeurs de InfraType (enum, validation, tests).
     L'infrastructure village suit les règles communes : une seule
     infrastructure par case (règle de la structure unique, GDD §3).
    - Transition achevée : P1.2c a restauré l'invariant final ;
      GameState.Validate exige désormais exactement 3 lettres pour tout
      territoire.
   - Mets à jour les tests (validation des enums, suppression du check
     owner d'infra, JSON round-trip).

2. GÉNÉRATEUR DE CARTE (internal/engine/mapgen) :
   - mapgen.Territory : retire LieuDit, ajoute Village bool `json:"village"`
     (true = village présent sur ce territoire à la création de la partie).
     Le flag est statique : il fait partie de map.json (la géographie est
     connaissance commune, inchangée après génération). Il sert de DONNÉE
     DE SEMIS : la création de partie (NewGame, P1.6/P1.7, et le demo state
     P1.2f) matérialise chaque flag en une entité Infrastructure de type
     village (SANS owner — le champ n'existe plus) sur le territoire non
     contrôlé. Le flag de la carte ne change JAMAIS (une case reste
     "village" dans map.json même si le château a remplacé le village : la
     couche dynamique fait foi pour l'état réel).
   - Remplace assignLieuDits par assignVillages :
     * Config : remplace LieuDitRatio par VillageCount int (≥ 1).
     * Contraintes : un village neutre n'est JAMAIS placé sur montagne ni
       marais (plaine, forêt, colline uniquement).
     * Répartition : déterministe, seedée, en maximisant la distance
       minimale entre villages (greedy : premier village aléatoire seedé,
       puis chacun maximise la distance au village neutre le plus proche —
       ou méthode équivalente documentée) → villages bien répartis sur la
       carte, pas groupés.
     * VillageCount par défaut au dev (cmd/server) : constante 5 pour
       l'instant (P1.2d introduira ?players=N et liera VillageCount = nb de
       joueurs + 1 ; les départs des joueurs P1.6 se font sur des villages
       distincts, d'où le +1).
   - NOMNAGE TRANSITOIRE (pont vers P1.2c, nécessaire pour retirer les
     dépendances au flag) : nameTerritories reprend le rôle des lieux-dits
     pour les villages — une case "village" reçoit le nom d'une commune
     (code trigramme), les autres cases reçoivent
     "{Qualificatif} de {Commune du village le plus proche/adjacent}"
     (motif existant). P1.2c remplacera tout ce système par un nommage
     "commune pour tous les territoires" : ne pas aller plus loin ici.
   - Tests mapgen : villages sur plaine/forêt/colline uniquement (plusieurs
     seeds), nombre = VillageCount, distance minimale respectée (seuil à
     définir, p.ex. ≥ 15 % de la diagonale du viewport entre deux villages
     ou un critère documenté équivalent), déterminisme byte-for-byte,
     aucun lieuDit résiduel.

3. FRONT (web/) :
   - types.ts : Territory.lieuDit → village ; InfraType + "village" ;
     Infrastructure sans owner (champ supprimé du type front).
   - TOUS les endroits qui lisent `territory.lieuDit` sont corrigés :
     notamment le panneau de détail de App.tsx (ligne "Lieu-dit") — il
     affiche désormais les infrastructures de la case (village/château/
     moulin…) depuis la couche état.
   - Les maps de labels exhaustives `Record<InfraType, string>` (App.tsx,
     MapViewer.tsx) reçoivent une entrée "village".
   - Rendu des villages : via les infrastructures de la couche état
     (state.json), PAS via le flag de carte — une case dont le village a
     été remplacé par un château ne doit pas afficher les deux. Icône/style
     distinct pour le village, légende mise à jour ("lieu-dit" →
     "village"). Les fixtures d'état sont mises à jour (suppression de
     lieuDit, infrastructures village le cas échéant).
   - npm run build && npm run lint.

4. RÉÉCRITURE DES DOCS (même dérogation que P1.2a : uniquement les fichiers
   listés, uniquement la substitution "lieu-dit" → "château/village" +
   la substitution "infrastructure du joueur" → "infrastructure sur une
   case contrôlée par le joueur" (les infrastructures n'ont plus de
   propriétaire) + reformulation minimale, SANS changer les règles de fond
   qui seront réécrites par P1.2e pour la production) :
   - specs/gdd.md :
     * §3 : "Lieux-dits et Maillage Territorial" → "Villages et Maillage
       Territorial" : le village est une infrastructure rare, neutre à
       l'origine, qui produit des rations et sert d'ancre de ravitaillement
       (détails P1.2e) ; le château remplit le même rôle (le château ne
       "transforme plus la case en lieu-dit" : il rend la case productive —
       même effet, nouvelle formulation) ; la "Règle de la Structure
       Unique" reste, et "construire un château sur un village le remplace".
     * §5 : "lignes de flux tracées automatiquement depuis les lieux-dits"
       → "depuis les châteaux et villages contrôlés" ; "Dépôt de Vivres du
       joueur" → "Dépôt de Vivres sur une case contrôlée par le joueur".
     * §7 : table des infrastructures — ajoute la ligne "Village" : coût
       "non constructible au MVP (réservé)" ; effet "Produit 2 rations par
       tour et sert d'ancre de ravitaillement (comme un château) ; un
       château construit sur un village le remplace" ; précise dans la
       table que les infrastructures appartiennent à leur case (pas de
       propriétaire).
     * §8 : "épuisement des stocks (lieux-dits contrôlés)" → "(châteaux et
       villages contrôlés)" ; "crédité au lieu-dit contrôlé le plus proche"
       → "au château ou village contrôlé le plus proche".
     * §2 : départ de partie "sur un lieu-dit distinct" → "sur un village
       distinct" (avec château auto-construit qui remplace le village —
       P1.6) ; paiement de proche en proche et conservation 50 % :
       "lieu-dit" → "château/village" ; rapatriement : "1 R par lieu-dit,
       2 R par château" → "1 R par village, 2 R par château".
     * §4 : "chaque lieu-dit contrôlé produit un rapport" → "chaque château
       ou village contrôlé produit un rapport" ; "Tour de Guet du joueur"
       → "Tour de Guet sur une case contrôlée par le joueur".
   - specs/architecture.md : contrat map.json ("lieuDit" → "village") ;
     state.json : Infrastructure sans champ "owner" ; ordres d'hiver : la
     formulation de `C C` (château rend la case productive, et remplace un
     éventuel village) est ajustée, aucune infrastructure village
     constructible (pas d'ordre `C V` au MVP — préciser "réservé" dans le
     commentaire contrat si pertinent) ; ordre P (pillage) : crédit au
     lieu-dit → au château/village contrôlé le plus proche.
   - specs/roadmap.md : P1.2 détaillé (item 6 : lieux-dits → villages
     neutres rares), P1.5 (sources = château/village), P1.6 (départ sur
     village distinct), P1.7 (rapports des châteaux/villages) ; décisions
     actées : ajouter "les infrastructures n'ont pas de propriétaire ;
     construire un château sur un village le remplace".
   - specs/prompts/p1.1-modeles-metier.md : IsLieuDit retiré du modèle
     Territory, OwnerID retiré d'Infrastructure, InfraType + village.
   - specs/prompts/p1.6-hiver-investissements.md : départ sur village
     distinct (château auto-construit qui remplace le village),
     conservation/rapatriement sur châteaux/villages.
   - specs/prompts/p1.7-rapport-tour-hotseat.md : sources de rapports =
     châteaux/villages contrôlés.
   - specs/prompts/p1.5-ravitaillement-famine.md : uniquement les
     substitutions "lieu-dit (contrôlé)" → "château/village (contrôlé)" et
     "Dépôt de Vivres du joueur" → "Dépôt de Vivres sur une case contrôlée
     par le joueur" — les règles complètes (rations, P1.2e) seront
     réécrites par le prompt P1.2e ; ne pas les devancer.
   - specs/prompts/p2.2-messagers-vision-tx.md : "Tour de Guet du joueur"
     → "Tour de Guet sur une case contrôlée par le joueur".
   - specs/prompts/p1.2-generation-carte.md : LAISSÉ TEL QUEL (historique).

5. Critères d'acceptation :
   - make test passe (modèles + mapgen), make vet passe
   - GET /api/map : plus aucun "lieuDit", des territoires portent
     "village": true (nombre = VillageCount, aucun en montagne/marais)
   - make web-dev : le front compile sans erreur console ; les villages
     s'affichent via la couche état (fixture) avec leur icône, légende OK,
     panneau de détail sans ligne "Lieu-dit" ; npm build/lint OK
   - Aucune occurrence de "lieu-dit"/"LieuDit"/"lieuDit" dans internal/,
     web/src, ni dans les fichiers specs/prompts réécrits (hors historique
     p1.2)
   - Aucun champ "owner" sur les infrastructures dans le JSON (modèles)

Note : documente dans ta réponse finale les choix ouverts que tu as
tranchés (seuil de distance entre villages, répartition greedy, valeur par
défaut du dev) pour mise à jour des specs. Ne commite pas : je m'en charge.
```
